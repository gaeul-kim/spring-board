<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
	<select id="selectUserId" parameterType="sampleProject.user.domain.User"
		resultType="Integer">
		<![CDATA[
			SELECT count(user_id) count 
			     FROM USER 
			         WHERE user_id = #{user_id}
	    ]]>
	</select>

	<insert id="insertUser" parameterType="sampleProject.user.domain.User"
		useGeneratedKeys="true" keyProperty="user_idx">
        <![CDATA[
            INSERT USER 
                (user_id, user_password, user_name, user_email, insert_date)
            VALUES 
                (#{user_id}, #{user_password}, #{user_name}, #{user_email}, now()) 
         ]]>
	</insert>
</mapper>
